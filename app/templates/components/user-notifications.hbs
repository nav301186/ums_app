{{nav-bar}}
<div id="notifications" class="container-fluid  margin-top-40">
  <div class="row">
    <div class="col-md-2">

    </div>
  <div class="col-md-8">
    <div class="card">
      <div class="card-block">
        <h4 class="card-title">Notifications</h4>
      </div>

      <ul class="list-group list-group-flush">
          {{#each model as |notification|}}
            {{notification-item sender=notification.intiator type=notification.type}}
          {{/each}}

      </ul>
    </div>
  </div>
  <div class="col-md-2">
<!-- {{filter-com}} -->
  </div>
</div>
</div>
