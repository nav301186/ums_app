
import { notificationType } from 'ums-app/helpers/notification-type';
import { module, test } from 'qunit';

module('Unit | Helper | notification type');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = notificationType([42]);
  assert.ok(result);
});

